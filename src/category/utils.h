#pragma once

/** Dummy struct
 * We use this struct when any value or type is needed,
 * e.g. when we are testing for existence of a function which accepts arbitrary
 * argument.
 */
struct dummy {};
